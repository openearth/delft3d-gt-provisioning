"""
Django provisionedsettings for delft3dcontainermanager project.

In here are all the settings which are dynamic on every
new provisioning run.
"""

CELERY_BROKER_URL = 'redis://:{{ redis_passwd }}@127.0.0.1:6379'
CELERY_RESULT_BACKEND = 'redis://:{{ redis_passwd }}@127.0.0.1:6379'
CELERY_ONCE = {
  'backend': 'celery_once.backends.Redis',
  'settings': {
    'url': 'redis://:{{ redis_passwd  }}@127.0.0.1:6379/0',
    'default_timeout': 60 * 60
  }
}
