---
# Global configuration
- name: apply common configuration to all nodes
  hosts: all
  gather_facts: no


  roles:
    - common
    - postgresql
    - apache
    - django
    - redis
    - django_app
    - subversion
    - docker
    - worker_delft3d
    - thredds

