---
ansible:
  playbook: test.yml
  verbose: vvvv

testinfra_dir: tests

docker:
  containers:
    - name: centos_test_host
      ansible_groups:
      - centos_group
      image: chrismeyers
      image_version: centos7
      privileged: True
      install_python: yes
