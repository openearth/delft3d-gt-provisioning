sudo: required

language: python

services:
  - vagrant

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -o Dpkg::Options::="--force-confold" --force-yes -y vagrant gcc ruby-dev
  - sudo vagrant plugin install vagrant-libvirt

install:
  - pip install molecule

script:
  - molecule test