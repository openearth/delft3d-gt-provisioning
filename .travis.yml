sudo: required
dist: trusty

language: python

# services:
  # - docker

# before_install:
  # - sudo apt-get -qq update
  # - sudo apt-get install -o Dpkg::Options::="--force-confold" --force-yes -y docker-engine software-properties-common python-selinux

install:
  - pip install ansible-lint ansible --upgrade

script:
  - ansible-playbook test.yml --check
  - ansible-lint test.yml
